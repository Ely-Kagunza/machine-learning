providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd Project/app", "python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "cd Project/app && gunicorn --config gunicorn.conf.py app:app"
